<%= form_for(@portfolio_item) do |form| %>
  <div class="row">
    <div class="col-md-6">
      <% if @portfolio_item.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@portfolio_item.errors.count, "error") %> prohibited this item from being saved:</h2>

          <ul>
          <% @portfolio_item.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= form.label :title %>
        <%= form.text_field :title, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :subtitle %>
        <%= form.text_field :subtitle, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.file_field :main_image, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.file_field :thumb_image, class: "form-control" %>
      </div>
    </div>
    <div class="col-md-6">
      <div class="form-group">
        <%= form.label :body %>
        <%= form.text_area :body, class: "form-control", rows: 15 %>
      </div>
    </div>
  </div>

  <hr>

  <div class="col-md-12">
    <h3>Technologies Used:</h3>
    <%= form.fields_for :technologies do |technology_form| %>
      <div>
        <%= technology_form.label :name %>
        <%= technology_form.text_field :name, class: "form-control" %>
      </div>
        <br>
    <% end %>
  </div>

  <div class="form-group">
    <%= form.submit 'Save Portfolio Item', class: "btn btn-primary" %>
  </div>
<% end %>

<%= link_to 'Back', portfolios_path %>
